'''
Programmer generate a four digit number using the random function and a
user need to guess this number. So for every digit, that the user guesses
the correct position, we print that one place is correctly guess.
cor = correct
nump = permanent number generated by the programmer.
'''

import random
nump = random.randint(1000, 9999)
# print(nump)    #If you print here then you saw the number.
n = int(input("Enter a four digit number"))
while n != 10:
    num = nump
    cor = 0
    while num % 10:
        numc = num % 10
        nc = n % 10
        num = num // 10
        n = n // 10
        if numc == nc:
            cor = cor + 1
    if cor == 4:
        print("Congrates! you guesses it right")
        break
    else:
        print("%d digits were guessed right" %cor)
        n = int(input("Enter a four digit nuber"))
else:
    print("You quit the game")
